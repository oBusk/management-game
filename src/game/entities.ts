import box from "./components/Box";
import hud from "./components/Hud";

function Entities() {
    const entities = {
        box,
        hud,
    };

    return entities;
}

export default Entities;
